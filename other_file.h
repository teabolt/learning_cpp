#ifndef OTHER_FILE_H_INCLUDED // an "include guard" makes sure that the header file is not inserted more than once in an implementation file
#define OTHER_FILE_H_INCLUDED



#endif // OTHER_FILE_H_INCLUDED

// this is like an interface
namespace other {
    int get_num();
}
